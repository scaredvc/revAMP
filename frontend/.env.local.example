# ---- Local-stack env (Docker Compose) ----
# Copy to frontend/.env.local before running: docker compose -f docker-compose.local.yml up

# Backend API URL — points at the backend container exposed on host port 8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google Maps API key (required for map features)
# Get one at https://console.cloud.google.com/ → APIs & Services → Credentials
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
